export type LoginResponseDto = {
    id: number
    email: string;
    username: string;
    token: string;
}

export type AuthResponseDto = {
    id: number
    email: string;
    username: string;
}
